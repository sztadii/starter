# Project docs

Durable product and process context for this repo.

| File | Purpose |
|------|---------|
| `000-business-requirements.md` | Product source of truth — `/init` → `/requirements`, then `/plan-next` |
| `001-development-workflow.md` | How we build: tasks, statuses, commands, gates |

Implementation backlog lives in `.cursor/tasks/` (see that README).
Agent lessons live in `.cursor/memory/` (see that README).
