# Agent memory

Project-specific lessons ("scars"), owned and maintained by the **main
agent only**. Plain Markdown — not mem0. Role agents do not read or write
this file; the main agent passes the relevant bullets when delegating.

Grouped by area (newest entry at the bottom of each section). Shape and
prune rules: `.cursor/memory/README.md`.

## Planning

- **Context:** Workflow felt slow with per-task prod deploy
  **Wrong:** Required production smoke before every `ready-for-qa`
  **Right:** Local verify + inspect only; `/deploy` when user asks
  **Detect:** Tempted to ship mid-task just to close QA

## Backend

No lessons yet.

## Frontend

No lessons yet.
